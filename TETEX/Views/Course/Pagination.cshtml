@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model List<TETEX.Models.Course>

<h1>Courses</h1>
@* <div class="table-responsive">
    <!-- Table content here -->
    <!-- Pagination -->
    <nav aria-label="Page navigation">
        <ul class="pagination">
            <li class="page-item @(Model.HasPreviousPage ? "" : "disabled")">
                <a class="page-link" href="@Url.Action("ListCourse", "Course", new { page = Model.PageIndex - 1 })">Previous</a>
            </li>
            <li class="page-item @(Model.HasNextPage ? "" : "disabled")">
                <a class="page-link" href="@Url.Action("ListCourse", "Course", new { page = Model.PageIndex + 1 })">Next</a>
            </li>
        </ul>
    </nav>
</div> *@
